# Máquina definitiva (VPS).
# 1a ejecución (SSH todavía en el 22):  añade  -e ansible_port=22
# Ejecuciones posteriores:               usa el 64295 (ya movido por el playbook)

[honeynet]
vps ansible_host=CAMBIA_POR_LA_IP ansible_user=root ansible_port=64295
